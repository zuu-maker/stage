import React from 'react';

const Text = ({text}) => {
    return (

            <div className='chat-text-container'>
                <span>{text}</span>

            </div>

    );
};

export default Text;